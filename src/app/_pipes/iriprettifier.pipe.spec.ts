import { IriprettifierPipe } from './iriprettifier.pipe';

describe('IriprettifierPipe', () => {
  it('create an instance', () => {
    const pipe = new IriprettifierPipe();
    expect(pipe).toBeTruthy();
  });
});
